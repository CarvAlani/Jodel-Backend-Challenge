version: '3'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      MONGO_HOST: "mongodb://mongo:27017/movies-api"
    restart: on-failure
    depends_on:
      - mongo
      - redis
    links:
      - mongo
      - redis
  mongo:
    image: mongo
    ports:
      - "27018:27017"
    volumes:
      - mongo-volume:/data/db
  redis:
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - redis-volume:/data
volumes:
  mongo-volume:
  redis-volume: